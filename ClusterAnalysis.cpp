/* Main program, which imports sdss dr10 galaxy data, uses this data to determine properties of the environment around clusters of interest, and finds correlations with reconstructed properties like concentration and mass with its environment. */

#include <iostream>

#include 'import_data.h'


int main{

  // Import sdss dr10 data from file

  // Segment sdss data around clusters from GR14.1.

  // Analyze the large scale structure around clusters

  // Determine if concentration and environment correlate
  
  return 0;
}
